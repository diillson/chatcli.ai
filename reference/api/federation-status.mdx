---
title: "Federation Status"
openapi: "GET /federation/status"
description: "Returns the global status of all federated clusters including connected/disconnected counts and total active issues"
icon: "network-wired"
---

<ResponseExample>
```json Response 200
{
  "apiVersion": "v1",
  "kind": "FederationStatus",
  "spec": {
    "totalClusters": 5,
    "connectedClusters": 4,
    "disconnectedClusters": 1,
    "totalActiveIssues": 7
  }
}
```
</ResponseExample>
